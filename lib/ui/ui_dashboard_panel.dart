import "package:flutter/material.dart";

class UIDashboardPanel extends StatelessWidget {
  const UIDashboardPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Dashboard Panel Coming Soon", style: TextStyle(fontSize: 22)),
    );
  }
}
